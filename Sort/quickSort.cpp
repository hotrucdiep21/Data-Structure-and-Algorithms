#include <iostream>
#include <vector>
#include<algorithm>

int partition(vector<int> &test, int n, int low, int high) {
    int pivot =   test[high];
    int  i = low-1;

    for(int j = low; j<high; j++) {
        if(arr[j]<pivot) {

        }
    }  
}

int main() {
    cout << 5/2

    return 0;
}
